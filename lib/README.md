# Hook Libraries

Shared utilities used by hook contracts. Pure functions and thin wrappers — no hook logic here.

## Modules

| Module | Purpose |
|--------|---------|
| `coding-standards-checks.ts` | Violation detection for coding standards (raw imports, try-catch, process.env, inline import types, as-any, relative imports, export default) |
| `svelte-utils.ts` | Svelte file classification (`isSvelteFile`) and `<script lang="ts">` block extraction (`extractSvelteScript`) with line-number-preserving padding |
| `narrative-reader.ts` | Reads narrative templates for hook messages |
| `signal-logger.ts` | Appends structured signals to JSONL files |
| `execution-classification.ts` | Command classification for execution evidence verification (state-changing vs read-only, output substantiveness, evidence reminders) |
| `time.ts` | Timestamp formatting (timezone via `TZ` env var, defaults to UTC) |

## coding-standards-checks.ts

Shared by two contracts:
- **CodingStandardsEnforcer** (PreToolUse) — blocks Edit/Write with violations
- **CodingStandardsAdvisor** (PostToolUse) — warns on Read of violating files

### File classification functions

- `isTypeScriptFile(path)` — matches `.ts`/`.tsx`/`.svelte` extensions
- `isAdapterFile(path)` — skips `adapters/` dirs, `adapter.ts` files, `hooks/core/`, `statusline/`
- `isAutoGeneratedFile(path)` — skips auto-generated directories (e.g. `module_bindings/` from SpacetimeDB)
- `isSkippedFilename(path)` — skips specific filenames (`vite.config.ts`, test fixtures) via `SKIP_FILENAMES` in `core/language-profiles.ts`

### Test file exceptions

Test files (`.test.ts`, `.spec.ts`, `.test.tsx`, `.spec.tsx`) are exempted from same-directory relative import checks in `findRelativeImports()`. Co-located tests legitimately import the module they test via `./MyModule`. Parent-directory traversals (`../foo`) are still enforced. All other violation categories still apply to test files.

### Framework-required exceptions

Some frameworks require `export default`. These are exempted in `findExportDefaults()`:

- `.config.(ts|js|mts|mjs)` files — build tool convention
- `spacetimedb/src/index.ts` — SpacetimeDB's `spacetime generate` CLI requires `export default` on the schema object
- `.svelte` files — Svelte components use implicit default exports by framework convention
- `.storybook/` directory — Storybook config files (`main.ts`, `preview.ts`) require `export default`

### Svelte conventions

Svelte files (`.svelte`) receive special treatment:

- **Relative imports** of `.svelte` files are exempted in `findRelativeImports()` — Svelte components are imported relatively by convention (e.g. `import Page from './Page.svelte'`)
- **Default exports** are exempted — Svelte components use implicit default exports
- **Script extraction**: Guards use `extractSvelteScript()` from `svelte-utils.ts` to extract only the `<script lang="ts">` block before scanning, avoiding false positives from HTML template and `<style>` sections
- **Svelte 5 runes** (`$state`, `$derived`, `$effect`, `$props`, `$bindable`) are valid Svelte syntax and do not trigger any-type violations

### Adding new auto-generated exclusions

Add a regex to the `AUTO_GENERATED_DIRS` array in `coding-standards-checks.ts`. Both Enforcer and Advisor inherit it automatically.
